function toggle_night_mode
    # Проверяем, запущен ли процесс
    if pgrep -x "hyprsunset" > /dev/null
        # Если запущен — убиваем, возвращая нормальный цвет
        killall hyprsunset
        notify-send -u low "Night Mode" "Выключен ☀️"
    else
        # Если не запущен — запускаем в фоне
        # -t 3000 — температура (чем меньше число, тем краснее экран)
        hyprsunset -t 3000 &
        notify-send -u low "Night Mode" "Включен 🌙"
    end
end
